work{^} {.};

:benchmark{n{^}100; ops{^}10; sequence_using_sends} {
    {{.}} (work[]) (
    {{.}} (work[]) (
    {{.}} (work[]) (
    {{.}} (work[]) (
    {{.}} (work[]) (
    {{.}} (work[]) (
    {{.}} (work[]) (
    {{.}} (work[]) (
    {{.}} (work[])
          (work[])
    ))))))))
};

:benchmark{n{^}100; ops{^}10; sequence_using_semis} {
    work[];
    work[];
    work[];
    work[];
    work[];
    work[];
    work[];
    work[];
    work[];
    work[];
    .
};

.
