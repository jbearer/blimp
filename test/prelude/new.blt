{import|.} std;

{do|

    bind class set_x (
        x{:=| (that{.get|.}){.eval|.} };
        y
    );
    bind class get_x (
        x{.get|.}
    );

    x <- 0;
    c <- (new class);

    ($ c): set_x 1;

    (($ c): get_x[]) {!expect|1};
        # Check that the instance variable got set as expected.
    ($ x) {!expect|0};
        # Check that it didn't capture our `x`.
    .

}: run[];

.
