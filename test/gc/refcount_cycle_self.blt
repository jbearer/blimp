#: -fno-gc-tracing -fno-gc-cycle-detection

{
    # `obj` will be a getter for...
    obj{
        ^({^value
            {(^value)}
        } {^self # A setter for `self`
            self {^ (^self)}
        })
    };

    # Set `obj:self` to point at `obj`, creating a self-cycle.
    obj[] (obj[])
}[];

!gc_print_stats;
!gc_expect_clean;
.
