# What happens when `this` and `that` in a receiver are inherited by a parent
# of the receiver?

bind foo bar .;

{do|
    this{:=|a};
    that{:=|b};

    {foo|c}{bar|d};

    this{.get|.}{.eval|.}{!expect|c};
    that{.get|.}{.eval|.}{!expect|d};

    .
}
